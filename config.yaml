project_name: "Kalashnikov Boosting Pipeline"

paths:
  raw_consumption: "data/raw/consumption.csv"
  raw_production: "data/raw/production.csv"
  task: "data/raw/task.csv"
  combined_data: "data/interim/combined.csv"
  clear_data: "data/processed/clear.csv"
  filtered_data: "data/processed/filtered.csv"
  features_data: "data/processed/features.csv"
  task_output: "data/processed/output.csv"
  models_dir: "models"
  reports_dir: "reports"
  state_file: "pipeline_state.json"

features:
  target_column: "Материал"
  date_column: "Дата"
  shop_column: "Цех"
  drop_columns: ["id документа_prod", "id документа_cons"]
  outlier_threshold: 1000000000000000000000.5 # Z-score для фильтрации выбросов

training:
  test_size: 0.2
  val_size: 0.2
  random_state: 42
  optuna_trials: 1
  models: ["xgboost", "lightgbm", "catboost"]

# Параметры чтения (автоматически определяются или задаются здесь)
csv_params:
  encoding: "utf-8"
  sep: ","

task:
  model: "lightgbm"